@page
@model PostsPageModel
@{
    ViewData["Title"] = "All";
}
<h1>@ViewData["Title"]</h1>

@if (Model.Posts == null)
{
    @("No posts found")
}
else
{
    foreach (var post in Model.Posts)
    {
        <article>
            <header>
                <h2><a href="@post.PostUrl">@Html.DisplayFor(modelItem => post.Title)</a></h2>
                <span class="post-category">@Html.DisplayFor(modelItem => post.PostType.Name)</span> ·
                <span class="post-author">@Html.DisplayFor(modelItem => post.AuthorsString)</span> ·
                <span class="post-date">@Html.DisplayFor(modelItem => post.DatePublished)</span>
            </header>
        </article>
    }
}
